import Attributes from '../../../shared/domain/value-objects/Attributes';

class RAOffensiveRebound extends Attributes {
  constructor(value: number) {
    super(value, 'offensiveRebound');
  }
}

export default RAOffensiveRebound;
